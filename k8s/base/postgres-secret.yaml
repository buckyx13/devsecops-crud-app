# DO NOT commit real secret values. Use AWS Secrets Manager + External Secrets
# Operator, or `kubectl create secret` / sealed-secrets in real deployments.
# This file is a TEMPLATE showing required keys; placeholder values only.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: crud-app
type: Opaque
stringData:
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: CHANGE_ME_USE_SECRETS_MANAGER
  POSTGRES_DB: cruddb
  PGUSER: appuser
  PGPASSWORD: CHANGE_ME_USE_SECRETS_MANAGER
  PGDATABASE: cruddb
  PGHOST: postgres
  PGPORT: "5432"
